# Never commit machine-local overrides
*.local

# Claude Code — ignore runtime state but version the stow package
.claude/
!claude/.claude/
!claude/.claude/**
# settings.json contains machine-specific permissions, keep local-only
claude/.claude/settings.json

# macOS
.DS_Store

# Editor
.idea/
*.swp
